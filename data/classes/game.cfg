{
	generate_unit: "def(string id) ->map
		map<- fold(map((map<- doc[id]), {(key): if(value is string and value != '' and value[0] = '$', eval(value[1:]), value)} ), a+b, {}) + {id: id}
		where doc = map<- get_document('data/units.cfg')
	",
}
